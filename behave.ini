# Behave configuration for BDD execution.
# This file tells Behave where to find the feature files and how to report results.
[behave]
paths = .
format = pretty
junit = true
junit_directory = reports
show_skipped = false
logging_config = logging.conf
